streamlit
groq
python-dotenv
easyocr
pymupdf
fpdf2
# Core
streamlit>=1.52.2
groq>=1.0.0
python-dotenv>=1.0.0

# Audio & Speech
pydub>=0.25.1
SpeechRecognition>=3.10.0
streamlit-mic-recorder>=0.0.8

# PDF
fpdf2>=2.8.5          # ← modern replacement, more reliable than fpdf

# Optional but very useful for stability
pillow>=10.0.0        # needed by many audio/image libs
numpy>=1.26.0         # often pulled by others anyway